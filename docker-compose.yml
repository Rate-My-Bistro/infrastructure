version: "3.7"

services:
    crawler:
        build: 
            context: rate-my-bistro-crawler
    

    server:
        build: 
            context: rate-my-bistro-server


    rethinkdb:
      image: rethinkdb:latest
      ports:
        - "8080:8080"
        - "29015:29015"
        - "28015:28015"
      volumes:
        - ./tls:/tls